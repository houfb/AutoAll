# AutoAll
## 🧭 功能用途
AutoAll 是一个工具基座，它主要用于实现像Windows计划任务一样，能定时启动和守护一些子工具进程，同时，为了常用与方便，
它还内置集成了一些特殊功能，比如自动注册开机自启动的计划任务、设置护眼色、Web站点更新工具等。它的主要目标帮助用户
简化一些日常工作，能让一些工作全自动或半自动去运行，将人从机械重复的工作中解脱出来。
 

## ✨ 功能特性

✔ 一个基座，若干组件，自由搭配和扩展，让多个进程可以像搭积木一样配合起来完成最终任务。

✔ 比Windows计划任务的配置操作更简洁、低门槛、人性化、强大。

✔ 让组件进程更聚焦于完成某个特定任务，让[开机自启、进程守护、软件更新、任务编排]这些通用、重复、边角料的工作由这个基座来统一完成。

✔ 通过内置功能实现最基本最常用的功能，你只需要进程少许配置打开他们的开关即可实现。

✔ 通过相关的配置节点如Coms等直接将其他进程配置为AutoAll的组件来自由扩展它的功能，然后编排他们的工作时间和顺序。
 
 
## ⚛️ 运行方式

- 先将 AutoAll 注册为开机自启动，有些功能需要更高特权来执行（俗称管理员权限），故此，建议恒注册为以管理员权限启动，在服务器上，可以注册为0层会话的无UI进程。这些工作，在Windows8以上，你只需要在配置文件中打开一个配置开关，AutoAll在首次启动时会自动注册(需要管理员权限来启动)，在系统不自带PowerShell5的Win7/Server2008上，你需要手动去配置计划任务，这也是AutoAll为啥要像计划任务却又执意要单写的原因之一，计划任务与操作系统和PowerShell版本的绑定太深了。

- 几乎所有组件的配置项都可以在配置文件变更时，自动载入新组件，新旧组件之间会有一个串行化交接逻辑，以保证他们的工作不会打架。

- 组件与组件之间，可以通过全局变量等内置通道做数据交互，以让组件与组件之间的工具能够交互和连贯。
 
- HTTP支持的变更需要重启软件，它不会随配置变更而切换是否打开或关闭HTTP监听。

- 但不要希寄工具能帮你100%完成一切自动化，但它却在努力帮你实现完成99%的工作，那1%的无法自动处理的异常总也无法杜绝，望提前明白。

  

## 🚀 使用方法
 
### 第1步：向开发者寻求授权  

虽然此工具在原理上不复杂，但实际开发过程中却非常磨人，开发者也是花费了大量精力的，也希望借此有少许收益。好在，我并不全靠此过活，所以，收费非常便宜，收费标准如下：  

|收费价格|授权范围|
|-|-|
|1¥|任意单个版本的版权，终身买断|

> 联系邮箱：1731321614@qq.com  微信号：HouFeiBiao

拿到的授权是一个字符串，你需要将它写入 AutoAll.exe 所在目录的配置文件 appsettings.json , 如果没有，请新建一个，注意编码用UTF8, 配置内容如下：
```
{ 
  //软件授权证书：可无证书运行7天体验期，若要长期使用，请联系作者获取授权证书。
  "Licence": "AAFm0v9bpaBFyb..." 
}
```


### 第2步：生成 SKey、GKey、TRaw

这三个是一切HTTP相关功能的安全基石，虽不是每个功能都需要，但凡是用到HTTP来实现的功能，几乎总是需要他们，所以，建议直接无脑配置一下。
 
打开debug模式，并启用HTTP，才能调用特殊链接生成RSA公私钥和TicketRaw（以下皆简称TRaw），配置文件中添加如下配置：
```
{ 
  "IsDebug": true,          //调用 MakeKeys MakeTicket MakeTicketRaw MakeJwt ，以及需HTTP输出StackTrace时，皆需将此置于 true。
  "Urls": "http://*:7788"   //HTTP监听地址，多个时，示例："http://*:7788;https://*:8877;http://127.0.0.1:7788;" 。凡需要HTTP服务端的功能皆须配置此项，否则不启动HTTP服务端，WebUpdater客户端也无法使用。
}
```

然后双击启动 AutoAll.exe ，然后找个浏览器，输入以下链接，可生成一套新的RSA公私钥，请勿必妥善保存它们，然后取其中的 PKCS#8 私钥配置给你的服务端上的 SKey ，取 PKCS#8 公钥(SPKI) 配置给你的客户端上的 GKey：  
http://127.0.0.1:7788/MakeKeys

将上述生成的 SKey 配好后，再调用以下链接，可得到一个 TicketRaw ，简称 TRaw , 把它配置到你的客户端中，这一步实际上就是让你的服务端给你的客户端授权：  
http://127.0.0.1:7788/MakeTicketRaw?sub=1&tokens=WebUpdater&exp=2026-12-31

· 上边的sub表示授权给谁，你可以任意取名，英文字母和数字皆可。
· tokens表示具体授权能用啥功能，AutoAll.exe 里有多项功能，如 WebUpdater 就是其中之一。
· exp是授权截止时间点，也支持任意ISO往返格式写法，但通常如上简洁书写即可，若你不写，默认10年。


最终的服务端配置如下：(仅在有需要HTTP服务端与客户端概念时才需要这个)
```
{ 
  "Licence": "AAFm0v9bpaBFyb...",
  "IsDebug": true,
  "Urls": "http://*:7788",
  "SKey": "AAFm0v9bpaBFyb..."
}
```

最终的客户端配置如下：(若不需要HTTP服务端，则不需要生成RSA公私钥和TRaw，自然也不需要配置 Urls GKey TRaw 这些)
```
{ 
  "Licence": "AAFm0v9bpaBFyb...",
  "IsDebug": true,
  "Urls": "http://*:7788",
  "GKey": "AAFm0v9bpaBFyb...",
  "TRaw": "AAFm0v9bpaBFyb..."
}
```

### 设置开机自启动

配置方法：
```
{
  //注册开机自启动：0|true表示开机时启动，运行在SYSTEM账户下，无UI界面；1表示在用户登录时启动，运行在具体用户账户下，有UI界面，在账户退出时停止；2表示混合模式，即主进程在SYSTEM下运行，但在每个用户登录时，会启动一个辅助进程，用于实现UI相关能力。 整个功能基于Windows计划任务实现，因此，由于OS版本和PS版本的差异，目前仅完整支持 Win10+|Server2012+&PowerShell5+ ，对于低版本的OS或PS，暂时可以通过手动添加计划任务来实现。
  "AutoStart": 1,  
}  
```

> - 配置了上述配置项后，需要在首次启动时，右键使用管理员权限启动 AutoAll.exe ，才能有权限写入计划任务。
> - 上述提到的主副进程混合模式，将在稍后的版本中，考虑逐步弃用，改用直接从无UI的SYSTEM账户下，通过拷贝用户登录令牌方式在指定用户UI中启动UI进程。


AutoAll 自身能够在操作系统开机时自动启动，是一切它能全自动工作运转，并管理其他进程的基础，但这个事实际上并不简单，在各种操作系统版本上，它非常不统一，而 AutoAll 试图给诸子组件进程提供一种稳定可靠的开机自启动支持。

开机自启动的实现有大致4种手段，基于多种靠量，这里最终选择主要以Windows计划任务来实现，因为只有这种才可以自由穿换于UI与非UI两种模式之间，还可以真正贯穿 Visa+ 实现统一的[在开机时就能以管理员权限运行起来]。

但是，Windows计划任务的自动注入目前基于PowerShell脚本实现(虽也可以直接用C#操控COM实现，
但PS更简洁些，还有就是历史遗留就这样写了)，但是，PS脚本操作计划任务却很依赖操作系统和
PowerShell的版本，这是个不小的麻烦，它目前仅在 Win10 1607+ 上测试可行，稍后若有需要，
有考虑用C#直接操控COM来实现，而不再基于PS脚本或CMD命令了，以保持 Vista+ 上的一致体验。

目前现状：    

| OS版本 | PS版本 | 现状及原因 |
| - | - | - |
|Windows7/Server2008 | 2.0 | 自带PowerShell2.0和CMD都无法正常写入计划任务，需要你手动将它添加到计划任务中，相关操作方法，不是一句能讲完，故需你自行搜索学习。|
|Windows8/Server2012 | 3.0 | 无ScheduledTasks模块，无法正常写入计划任务，需要你手动添加计划任务|
|Windows8.1/Server2012R2|4.0|ScheduledTasks模块半残，手动安装PS5.1也不行，需要你手动添加计划任务|
|Windows10/Server2016| 5.1 | ScheduledTasks模块缺损，因取消不掉[最大运行时间限制3天]，建议你手动添加计划任务|
|Windows11/Server2019| 5.1 | ScheduledTasks模块正常，建议你直接使用配置项让 AutoAll 帮你自动写入|

  


### 设置护眼色

配置方法：
```
{
  //true表示要设置护眼色, 若为字串，如 "202 234 206"|"0 202 234 206"，表示要设置为指定的RGB|ARGB颜色值，否则将使用默认的护眼色值"202 234 206"。
  "EyesColor": true 
}
```

> - 这是一个历史遗留功能，它从 AutoAll 的初代版本就有，甚至可以说是 AutoAll 的诞生之源，虽在 Win11 上不太好使了，但作为仍有少许价值的纪念品仍被保留下来了。
> - 相似的功能还有一个 [干掉WPS广告] 的功能，但几经更迭后，发现总是误伤一些WPS中的正常弹框功能，并且，后来发现WPS实际上是提供了一个设置面板来手动关掉WPS的广告弹窗的，几经权衡，自 AutoAll_v1.2.0.0 开始就去掉了此功能，但是，若有一天WPS太过放飞自我，不排除它再次回归的可能，因为这个工具从诞生之初就不是一个运维工具，而是一个让用户自我爽起来的工具。
 

### 查看日志
 
配置方法：
```json
{
  //日志标志，特殊值：
  //-1 输出到控制台；-2 输出到文件中；-3 消息前带时间；-4 消息前带日期和时间；+ 表示[-3, -2, -1, 3]；
  //6 None 不记录任何日志 > 5 Critical 严重错误 > 4 Error 错误信息 > 3 Warning 警告信息 > 2 Information 一般信息 > 1 Debug 调试信息 > 0 Trace 最详细 ，(当启用更详细的细粒度级别时，会自动包含启用其上级的更粗粒度级别)；
  //其他日志输出标志，按模块关注点分类，以手动控制只输出某些模块关注点的日志来便于调试。  
  "LogSigns": "+ 0 1 2 3 4 5 260517", 
  //这是.NET原生支持的日志级别，这里同样支持，但这个只能输出到控制台，且只能输入.NET原生的行为日志。
  "Logging": {
    "LogLevel": { //Trace 最详细 < Debug 调试信息 < Information 一般信息 < Warning 警告信息 < Error 错误信息 < Critical 严重错误 < None 不记录任何日志 
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"  
    }
  }
}
```

因为 AutoAll 的设计目标是安装之后，一切全自动无UI运行，连错误都要尽可能自我修复或重置，配置变更都要尽可能在
运行时热重载，所以，日志就是主要的用来观测它是否运行良好的几乎唯一的方式。

但是，AutoAll 内部有多个功能，且它们实质上总是可能要并行运行的，此时，把日志糊到一堆，或输出到不同的文件中，
是十分不优雅或难管理的，这里选择的方式时，建议你在正式环境中，只将日志级别设置到[Warning 警告]级别，然后仅
在出了问题需要调试时，才将日志级别调到[Information 一般]或更细粒度，以及只打开某一个模块的日志，
比如 "0 260517" 就表示，我仅想看 [WebUpdater模块] 最详细级别的日志，其他模块除非遇到极严重错误，否则一律不
输出。


> 由于配置文件的载入时机相对靠后，若想观测配置文件载入前的详细日志，可以使用启动参数 --Log 或 --LogSigns 来指定初始化时的日志级别。
> ```
> start "" AutoAll.exe --log + 0   :: + 表示[-3, -2, -1, 3], 0 表示最详细级别
> ```


### 启动参数

- 支持 --hide|--h 来主动隐藏窗口  
- 支持 --port|--u 来调整HTTP监听端口  
- 支持 --urls|--u 来调整HTTP监听地址(优先级低于--port)  
- 支持 --dev|-dev 来开启开发者模式(此时会载入appsettings.development.json)  
- 支持 --rerun|-r 来控制在启动时立即自我重启一次，主要是为了适应让计划任务在启动此程序后，在计划任务面板上可以看到此程序是已完成状态，而不是进行中的状态，在低版本PowerShell或PowerShell不能完全实现将[仅交流电源时执行][运行超过N天后自动停止]这两项自动钩掉时，这将是非常有用的，这两项暂时还无法实现在用PS脚本写入计划任务时自动钩掉，暂只能手动在UI界面上实现，但，自我重启后，就不再受这个计划任务管控了。  
- 支持 --log|-log 若想观察配置文件载入前的运行日志，可用启动命令来注入初始化的日志级别  
     